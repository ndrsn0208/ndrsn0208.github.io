import { ComingSoon } from './Blog'

export default function Photography() {
  return (
    <ComingSoon
      section="Photography"
      hint="A small set of photographs — landscapes, slow walks, light through windows. Going up after I sort the archive."
    />
  )
}
